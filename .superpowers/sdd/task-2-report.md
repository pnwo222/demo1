# Task 2 实现报告

## 最终状态

DONE

## 最终 Product 审查修复（2026-07-15）

- 独立提交：`0fc4cdb docs: align fyy prd menu with prototype`。
- 以 `docs/design/FY-20260714-UNICARD-admin-page-blueprint.md` 的菜单映射为唯一来源，将 PRD 菜单表从聚合行展开为 49 个独立后管页面，每页单独记录需求编号、一级/二级/三级菜单、路由、权限、可见角色、页面类型和 Snowy 关系。
- 路由全部统一为 `/unicard/school/**`、`/unicard/platform/**`、`/unicard/interface/**`、`/unicard/pam/**`；权限全部统一为 `unicardSchool*`、`unicardPlatform*`、`unicardInterface*`、`unicardPam*` 体系。
- 逐页角色与蓝图及原型一致，包括社保卡申领记录“学校管理员、安全审计人员”、学校设备状态监控“学校管理员、设备运维人员”、平台对接“平台管理员、安全审计人员”、审核中心“平台管理员、审核人员”、系统监控“平台管理员、设备运维人员、安全审计人员”。
- 自动解析 PRD 菜单表、蓝图首个菜单映射和原型 `pageSpecs`，对菜单层级、路由、权限、角色及页面类型进行三方比对：`PRD=49，蓝图=49，原型=49，差异=0`。
- 旧 `/fy/` 与 `fy:` 路由/权限残留检查：Markdown、HTML 均为 0。
- 回归检查：62 项编号与 H5 固定延期原因通过；Markdown/HTML 各 12 张表列数通过；49 行菜单数量通过；17 个同步/只读页面操作列无新增、编辑、删除；HTML 单文件结构与 UTF-8 检查通过。

## 审查修复（2026-07-15）

- 修复后独立提交：`3ffea86 docs: fix fyy prd coverage and menu routes`。
- 将菜单表中含裸 `|` 的聚合路由全部改成 `<br>` 分隔的逐路由、逐权限表达，补充顺序对应关系；Markdown 与 HTML 的菜单、路由、权限、角色和 Snowy 关系保持一致。
- 补齐审查指出的 ADM-S-004 当前状态/卡规范版本、ADM-S-012 参保地/卡规范版本、ADM-S-015 审核状态/预约渠道/被访人筛选、ADM-S-017 支付时间筛选、ADM-S-018 姓名/书籍题名/条码号筛选。
- 全量复核 62 项语义，并进一步补齐 H5 保留能力详情、学生/历史学生筛选、学校和平台首页时间筛选、师生社保卡状态、文章发布去向、角色启停、外部调用日志筛选等。
- 移除 ADM-P-005 中标书未明确的模块新增、编辑、删除能力，明确只配置需求给出的显示名称、图标、排序及菜单/权限支撑关系。
- 明确 API-001～API-008 共同执行格式校验、重复项检测、异常数据拦截，并支撑初始化、实时交互、场景应用和数据反哺闭环。

### 审查修复验证

| 检查 | 结果 |
| --- | --- |
| 原任务编号和 H5 延期检查 | PASS |
| Markdown/HTML 全量编号 | 62/62，PASS |
| 62 项逐行语义清单 | H5 13 + 学校端 20 + 管理端 13 + API 8 + PAM 8，PASS |
| API 共同闭环语义 | 7/7，PASS |
| Markdown 表格列数 | 12 张表，PASS |
| HTML 表格列数 | 12 张表，PASS |
| 裸竖线聚合路由检查 | PASS，无残留 |
| 审查 P1 指定字段/筛选断言 | PASS |
| UTF-8 替换字符检查 | PASS |
| HTML doctype、内联 CSS、闭合标签 | PASS |
| `git diff --check` / `git diff --cached --check` | PASS |
| 提交范围 | 仅两份 PRD 文件 |

## 执行范围

- 生成 `docs/prd/FY-20260714-UNICARD-prd.md`。
- 生成 `docs/prd/FY-20260714-UNICARD-prd.html`，使用内联 CSS、无外部构建依赖。
- 只读核对历史 PRD、当前 62 项需求基线与已确认 Product 规格。
- 未读取、修改、暂存或提交 `project/h5/`。

## 关键处理

- 覆盖 H5 13 项、学校端 20 项、管理端 13 项、接口 8 项、PAM 8 项，共 62 项。
- H5-001～H5-013 全部标记“需求保留、设计延期”，逐项使用固定原因“开发者明确本阶段暂不考虑 H5 设计，且不采用当前未跟踪 project/h5 作为正式基线”。
- 将学生、教职工、历史学生、申领/访客/通行/消费/借阅记录、日志和对接监控等限定为同步或只读页面，没有继承历史 PRD 中不恰当的万能 CRUD。
- 明确学校数据隔离、服务端鉴权、身份证/手机/社保卡脱敏、掌静脉模板受控展示、敏感导出审计。
- 明确 API-007/API-008 的签名、幂等、金额校验、重复推送、失败重试和对账要求。
- 明确批量导入导出、权限授权、删除和设备远程配置的二次确认、影响范围、逐项结果和审计要求。

## 校验命令与结果

1. 历史内容核对：
   - `git show 'codex/fyy-requirement-breakdown:docs/prd/FY-20260708-UNICARD-prd.md' | Select-Object -First 160`
   - 结果：PASS；历史材料可读取，并识别出同步页面 CRUD 需要纠正、H5 结论不可继承。
2. Brief 指定覆盖检查：
   - 对 Markdown/HTML 检查 `H5-001`、`H5-013`、`ADM-S-001`、`ADM-S-020`、`ADM-P-001`、`ADM-P-013`、`API-001`、`API-008`、`PAM-001`、`PAM-008` 及延期状态。
   - 结果：PASS，退出码 0。
3. 全量原子编号检查：
   - 程序化构造 62 个编号并分别检查 Markdown/HTML。
   - 结果：两文件均 `atomic=62 missing=0`。
4. H5 决策检查：
   - 两文件均包含 13 条 H5 表格状态；固定原因在每条 H5 记录中出现。
   - 统计输出：`defer=17 reason=14`（除 13 条记录外，章节说明/范围/验收还会重复出现决策文本）。
5. 规定章节检查：
   - 对任务简报要求的 18 个章节逐一检查。
   - 结果：两文件均 `sections=18 PASS`。
6. Git 文本检查：
   - `git diff --check -- ...`
   - `git diff --cached --check`
   - 结果：PASS，无空白错误。
7. 提交范围检查：
   - 提交前 `git diff --cached --name-only` 必须且仅能得到两个目标 PRD 文件。
   - 结果：PASS。

## 产物统计

| 文件 | UTF-8 字节数（提交前校验） | 原子编号 | 章节 |
| --- | ---: | ---: | ---: |
| `docs/prd/FY-20260714-UNICARD-prd.md` | 26,550 | 62/62 | 18/18 |
| `docs/prd/FY-20260714-UNICARD-prd.html` | 33,014 | 62/62 | 18/18 |

## 提交

- Commit：`5df4532 docs: add fyy unicard product requirements`
- 提交文件：仅两个当前 PRD 文件。

## 残余风险

- 校园数据中台、宁波一卡通、社保卡、访客、图书和设备协议尚未提供，接口字段、错误码、签名算法、SLA 需在技术方案阶段确认。
- 内容审核状态机、敏感数据留存/明文审批、PAM 内建或外接边界、交易对账周期与设备命令回滚仍需业务、安全和架构角色确认。
- H5 需求仅保留追踪，本任务未生成 H5 蓝图、原型或实现，符合开发者明确延期决策。
