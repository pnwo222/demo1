# Food Delivery Backend

Java backend skeleton for the food delivery platform.

Current stage only confirms the engineering layout under `project/backend`.
Business APIs, database migrations, idempotency, order state machine, payment callbacks,
and security checks will be implemented by the assigned backend and data tasks.
