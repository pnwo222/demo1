```ts
import { getTabType } from '@/api/place/index'
import { openApiPicker } from '@/components/OpenApiPicker'

const form = ref({
  name: '1'
})

// 静态数据
openApiPicker({
  value: form,
  field: 'name',
  // van-picker 的参数
  props: {
    title: '选择公司',
    columns: []
  },
  onConfirm() {
    console.log(form.value.name)
  },
})

// 接口
openApiPicker({
  value: form,
  field: 'name',
  // 接口
  api: getTabType,
  // label 对应key
  labelField: 'locationDes',
  // value 对应key
  valueField: 'locationCode',
  // 返回数据层级, x.x.x
  // resultField:'',

  // 自定义数据格式处理
  // formatData: (columns) => {
  //   return columns
  // },
  props: {
    title: '检查场所类型',
  },
  onConfirm() {
    console.log(form.value.name)
  },
})
```
